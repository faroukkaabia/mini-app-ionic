// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase : {
    apiKey: 'AIzaSyBShPN_SwoFweLSAoBHYPSPPPPB_KYwsuw',
    authDomain: 'checklist-17526.firebaseapp.com',
    projectId: 'checklist-17526',
    storageBucket: 'checklist-17526.appspot.com',
    messagingSenderId: '103514981798',
    appId: '1:103514981798:web:30e0701ff1432a0f84fb67',
    measurementId: '${config.measurementId}'
  },

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
